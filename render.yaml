services:
  - type: web
    name: recuerdos-con-sabor-by-romi
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
